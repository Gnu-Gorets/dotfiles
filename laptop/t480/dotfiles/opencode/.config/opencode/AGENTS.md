# Global OpenCode defaults

- `pohuy` is loaded globally from its canonical `SKILL.md` via `opencode.jsonc`.
- Start `pohuy` at level `ultra` by default.
- Keep commands, code, paths, configuration, and other technical artifacts clean.
